- name: variables
  hosts: web
    vars:
      course: ansible
      duration: 20hrs
      title: sam

    tasks:
    - name 
    ansible.builtin.debug
      "hello iam learning {{course}} from sivakumar and the duration is {{duration}}, and the title is {{title}}"
